@page
@model RazorWithCodeFirst.Pages.Expenses.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Expense[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Expense[0].DateIncurred)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Expense[0].Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Expense[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Expense[0].ExpenseType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Expense[0].UserID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Expense) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateIncurred)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpenseType.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
